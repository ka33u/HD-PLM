# 代码与许可证

HD-PLM 为独立的「新品协同」应用，包含应用入口、数据库、账号权限、项目、BOM、制造准备、采购、问题和附件服务。

沿用此前开发的 NPI 业务规则、界面及相关测试，保留其 AGPL-3.0-or-later 许可标记与许可证。本仓库以独立源码建立，未导入旧仓库的提交历史。

React、Hono、Vite、Drizzle、ExcelJS、Radix UI 等通用开源库通过 npm 安装，各自许可证随依赖发布。它们不构成旧 PLM 应用运行依赖。
